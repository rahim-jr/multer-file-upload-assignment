# Multer + Express File Upload Example

## Quick start

1. Install dependencies:

   ```bash
   npm install
   ```

2. Run the server:

   ```bash
   node server.js
   ```

3. Open `http://localhost:3000`

## Notes

- Uploaded files are saved to `uploads/` directory.
- The example restricts uploads to images and limits file size to 2MB.
